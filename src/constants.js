/**
 * Zero transform object with identity values
 * @type {{x: number, y: number, xScale: number, yScale: number, rotation: number}}
 */
export const ZERO_TRANSFORM = {
  x: 0,
  y: 0,
  xScale: 1,
  yScale: 1,
  rotation: 0,
}
